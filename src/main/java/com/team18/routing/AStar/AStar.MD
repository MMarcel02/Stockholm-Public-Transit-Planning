1. Build a Stop adjacency structure (each stop has to know its neighbours) we can use the stopDepartures field to make that happen, because each stopID is mapped to its StopTime

2. Calculate the graph edge weight.

...